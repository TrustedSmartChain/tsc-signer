// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: lockup/v1/lock.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "lockup.v1";

export interface Lock {
  unlockDate: string;
  amount: string;
}

export interface Locks {
  locks: Lock[];
}

function createBaseLock(): Lock {
  return { unlockDate: "", amount: "" };
}

export const Lock: MessageFns<Lock> = {
  encode(message: Lock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.unlockDate !== "") {
      writer.uint32(10).string(message.unlockDate);
    }
    if (message.amount !== "") {
      writer.uint32(18).string(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.unlockDate = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lock {
    return {
      unlockDate: isSet(object.unlockDate)
        ? globalThis.String(object.unlockDate)
        : isSet(object.unlock_date)
        ? globalThis.String(object.unlock_date)
        : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
    };
  },

  toJSON(message: Lock): unknown {
    const obj: any = {};
    if (message.unlockDate !== "") {
      obj.unlockDate = message.unlockDate;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lock>, I>>(base?: I): Lock {
    return Lock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lock>, I>>(object: I): Lock {
    const message = createBaseLock();
    message.unlockDate = object.unlockDate ?? "";
    message.amount = object.amount ?? "";
    return message;
  },
};

function createBaseLocks(): Locks {
  return { locks: [] };
}

export const Locks: MessageFns<Locks> = {
  encode(message: Locks, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.locks) {
      Lock.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Locks {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocks();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.locks.push(Lock.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Locks {
    return { locks: globalThis.Array.isArray(object?.locks) ? object.locks.map((e: any) => Lock.fromJSON(e)) : [] };
  },

  toJSON(message: Locks): unknown {
    const obj: any = {};
    if (message.locks?.length) {
      obj.locks = message.locks.map((e) => Lock.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Locks>, I>>(base?: I): Locks {
    return Locks.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Locks>, I>>(object: I): Locks {
    const message = createBaseLocks();
    message.locks = object.locks?.map((e) => Lock.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
