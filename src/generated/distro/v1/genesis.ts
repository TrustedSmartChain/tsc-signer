// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: distro/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "distro.v1";

/** GenesisState defines the module genesis state */
export interface GenesisState {
  /** Params defines all the parameters of the module. */
  params?: Params | undefined;
}

/** Params defines the set of module parameters. */
export interface Params {
  mintingAddress: string;
  receivingAddress: string;
  denom: string;
  maxSupply: string;
  distributionStartDate: string;
  monthsInHalvingPeriod: string;
}

function createBaseGenesisState(): GenesisState {
  return { params: undefined };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseParams(): Params {
  return {
    mintingAddress: "",
    receivingAddress: "",
    denom: "",
    maxSupply: "",
    distributionStartDate: "",
    monthsInHalvingPeriod: "0",
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mintingAddress !== "") {
      writer.uint32(10).string(message.mintingAddress);
    }
    if (message.receivingAddress !== "") {
      writer.uint32(18).string(message.receivingAddress);
    }
    if (message.denom !== "") {
      writer.uint32(26).string(message.denom);
    }
    if (message.maxSupply !== "") {
      writer.uint32(34).string(message.maxSupply);
    }
    if (message.distributionStartDate !== "") {
      writer.uint32(42).string(message.distributionStartDate);
    }
    if (message.monthsInHalvingPeriod !== "0") {
      writer.uint32(48).uint64(message.monthsInHalvingPeriod);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mintingAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.receivingAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.denom = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.maxSupply = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.distributionStartDate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.monthsInHalvingPeriod = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      mintingAddress: isSet(object.mintingAddress)
        ? globalThis.String(object.mintingAddress)
        : isSet(object.minting_address)
        ? globalThis.String(object.minting_address)
        : "",
      receivingAddress: isSet(object.receivingAddress)
        ? globalThis.String(object.receivingAddress)
        : isSet(object.receiving_address)
        ? globalThis.String(object.receiving_address)
        : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      maxSupply: isSet(object.maxSupply)
        ? globalThis.String(object.maxSupply)
        : isSet(object.max_supply)
        ? globalThis.String(object.max_supply)
        : "",
      distributionStartDate: isSet(object.distributionStartDate)
        ? globalThis.String(object.distributionStartDate)
        : isSet(object.distribution_start_date)
        ? globalThis.String(object.distribution_start_date)
        : "",
      monthsInHalvingPeriod: isSet(object.monthsInHalvingPeriod)
        ? globalThis.String(object.monthsInHalvingPeriod)
        : isSet(object.months_in_halving_period)
        ? globalThis.String(object.months_in_halving_period)
        : "0",
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.mintingAddress !== "") {
      obj.mintingAddress = message.mintingAddress;
    }
    if (message.receivingAddress !== "") {
      obj.receivingAddress = message.receivingAddress;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.maxSupply !== "") {
      obj.maxSupply = message.maxSupply;
    }
    if (message.distributionStartDate !== "") {
      obj.distributionStartDate = message.distributionStartDate;
    }
    if (message.monthsInHalvingPeriod !== "0") {
      obj.monthsInHalvingPeriod = message.monthsInHalvingPeriod;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.mintingAddress = object.mintingAddress ?? "";
    message.receivingAddress = object.receivingAddress ?? "";
    message.denom = object.denom ?? "";
    message.maxSupply = object.maxSupply ?? "";
    message.distributionStartDate = object.distributionStartDate ?? "";
    message.monthsInHalvingPeriod = object.monthsInHalvingPeriod ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
